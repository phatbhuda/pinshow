<%= form_with model: board do |form| %>
  <div>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
    <% board.errors.full_messages_for(:title).each do |message| %>
      <div><%= message %></div>
    <% end %>
  </div>

  <div>
    <%= form.label :url %><br>
    <%= form.text_area :url %><br>
    <% board.errors.full_messages_for(:url).each do |message| %>
      <div><%= message %></div>
    <% end %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>